Откройте терминал (или командную строку) и выполните команды:
  pip install selenium
Если у вас нет chromedriver в PATH, рекомендуется также установить:
  pip install webdriver-manager

Код работает, если chromedriver находится в PATH.

Откройте терминал, перейдите в директорию с файлом и выполните команду:
  python test_form.py
